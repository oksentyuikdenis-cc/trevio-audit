{
  "name": "trevio-murano",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "//stage": "The demo build. `build` alone omits the search bar on purpose — that is the offline delivery guarantee, and it is why the default build makes zero network requests. This one turns the search on and binds to every interface, which is what lets an audience open it on their own phones over the venue wi-fi. Run it, then read the Network URL it prints.",
    "stage": "VITE_AUDIT_API=on vite build && vite preview --host --port 5184"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
